// import Vue from "vue";

const market = {
  namespaced: true,
  state: () => ({
    funds: 0,
  }),
  mutations: {
    updateTotal(state, data) {
      data.category
    }
  }
}

export default market;